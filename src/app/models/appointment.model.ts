export interface Appointment {
  date: string;
  time: string;
  title: string;
  desc: string;
}

export interface ToDo {
  title: string;
  priority: string;
  status: string;
  description: string;
}
